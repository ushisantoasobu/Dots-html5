describe('0', function() {
  it('trueが返ってくるか確認する', function() {
    expect(dots.MainCtrl.testTestem()).to.eql(true);
  });

  it('trueが返ってくるか確認する', function() {
    expect(dots.MainCtrl.testTestem()).to.eql(true);
  });
});